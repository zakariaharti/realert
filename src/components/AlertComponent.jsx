import React from 'react';

/* eslint-disable */

const AlertComponent = (props) => {
  const {
    isOpen,
    title,
    content,
    level,
    icon,
    allowHTML,
    buttons,
    dismissOnClickOutside,
    dismissOnEsc,
    dangerMode,
    dismissAfter,

  } = props;

  return (
    <div>gh</div>
  );
};

export default AlertComponent;
